Please run:
	# to set shell scripts executable
	$ chmod +x *.sh

	# to build Docker images
	$ ./build.sh

	# to start the Docker containers
	$ ./start.sh

	# (Optional) to test the Docker containers
	$ ./test.sh

==
Documentation:
The Api doc can be found here => http://localhost:3000/apidocs

You may also remove this file (README.NOW.txt) now!

